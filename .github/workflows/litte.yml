name: litte

on:
  push:
    paths:
      - '.github/workflows/litte.yml'
      - 'commit/**'
  schedule:
    - cron: 0 */4 * * *
  repository_dispatch:
  workflow_dispatch:
    inputs:
      packages:
        description: 'packages'
        required: false
        default: 'false'

jobs:
  job_litte:
    if: github.event.repository.owner.id == github.event.sender.id || ! github.event.sender.id
    runs-on: ubuntu-latest

    name: Update litte
    strategy:
      fail-fast: false
      matrix:
        target: [master]

    steps:
    - name: Checkout
      uses: actions/checkout@main
      with:
        fetch-depth: 0

    - name: Initialization environment
      run: |
        git config --global user.email "actions@github.com"
        git config --global user.name "action"
        sudo timedatectl set-timezone "Asia/Shanghai"

    - name: Clone packages
      run: |
        cd $GITHUB_WORKSPACE
        chmod +x .github/diy/${{matrix.target}}.sh
        git clone -b master https://github.com/kenzok8/litte.git ${{matrix.target}}
        cd ${{matrix.target}}
        # 保留 .git 和 .github，只删除包目录
        find . -mindepth 1 -maxdepth 1 ! -name '.git' ! -name '.github' -exec rm -rf {} +

        $GITHUB_WORKSPACE/.github/diy/litte.sh
        bash /$GITHUB_WORKSPACE/.github/diy/convert_translation.sh
        bash /$GITHUB_WORKSPACE/.github/diy/create_acl_for_luci.sh -a
        bash /$GITHUB_WORKSPACE/.github/diy/Modify.sh

    - name: Detect version-changed packages
      run: |
        cd $GITHUB_WORKSPACE/${{matrix.target}}
        : > /tmp/version_changed_packages.txt
        : > /tmp/package_version_map.txt

        get_ver() {
          awk '/^(PKG_VERSION|LUCI_VERSION)[[:space:]]*:?=/{
            sub(/^[^=]*=[[:space:]]*/,""); gsub(/[[:space:]]+$/,""); print; exit
          }' "$1"
        }

        # 直接遍历所有 Makefile，不依赖 git status（新包 untracked 也能捕获）
        for makefile in */Makefile; do
          [ -f "$makefile" ] || continue
          pkg=$(dirname "$makefile")
          new_ver=$(get_ver "$makefile")
          [ -z "$new_ver" ] && continue

          old_ver=""
          if git cat-file -e "HEAD:${makefile}" 2>/dev/null; then
            old_ver=$(git show "HEAD:${makefile}" | awk '/^(PKG_VERSION|LUCI_VERSION)[[:space:]]*:?=/{
              sub(/^[^=]*=[[:space:]]*/,""); gsub(/[[:space:]]+$/,""); print; exit
            }')
          fi

          if [ "$new_ver" != "$old_ver" ]; then
            echo "$pkg" >> /tmp/version_changed_packages.txt
            echo "$pkg|$new_ver" >> /tmp/package_version_map.txt
          fi
        done

        sort -u /tmp/version_changed_packages.txt -o /tmp/version_changed_packages.txt
        echo "版本变更包: $(wc -l < /tmp/version_changed_packages.txt) 个"
        cat /tmp/package_version_map.txt

    - name: 🚀 Upload
      env:
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }}
      run: |
        cd $GITHUB_WORKSPACE/${{matrix.target}}

        # 全量 add，识别所有变更
        git add -A

        if git diff --cached --quiet; then
          echo "ℹ️ nothing to commit"
          exit 0
        fi

        # 获取版本号（从版本变更表）
        get_pkg_version() {
          local pkg="$1"
          [ -s /tmp/package_version_map.txt ] || { echo ""; return; }
          awk -F'|' -v p="$pkg" '$1==p{print $2; exit}' /tmp/package_version_map.txt
        }

        # 获取上游 commit 信息（从 litte.sh 预先保存的文件）
        get_upstream_msg() {
          local pkg="$1"
          [ -s /tmp/upstream_commit_msgs.txt ] || { echo ""; return; }
          awk -F'|' -v p="$pkg" '$1==p{
            # 去掉 "pkgname: " 前缀避免重复
            sub(/^[^|]*\|/,""); print; exit
          }' /tmp/upstream_commit_msgs.txt
        }

        # 获取所有变更的顶层包目录（排除 .github）
        all_changed=$(git diff --cached --name-only -- . ':(exclude).github/**' \
          | awk -F/ 'NF>1{print $1}' | sort -u)

        # 逐包单独提交
        git reset HEAD -- . 2>/dev/null || git reset 2>/dev/null || true
        committed_any=false

        while IFS= read -r pkg; do
          [ -z "$pkg" ] && continue

          # 暂存该包的所有变更（新增/修改/删除）
          git add -A -- "$pkg/" 2>/dev/null || true

          git diff --cached --quiet && continue

          ver=$(get_pkg_version "$pkg")
          if [ -n "$ver" ]; then
            commit_msg="📦 $pkg: update to $ver"
          else
            upstream_msg=$(get_upstream_msg "$pkg")
            # 去掉上游信息中可能的 "pkgname: " 重复前缀
            upstream_msg=$(echo "$upstream_msg" | sed "s/^${pkg}[[:space:]]*:[[:space:]]*//" | sed 's/^[[:space:]]*//')
            if [ -n "$upstream_msg" ]; then
              commit_msg="📦 $pkg: $upstream_msg"
            else
              commit_msg="📦 $pkg: sync upstream"
            fi
          fi

          git commit -m "$commit_msg"
          echo "✅ $commit_msg"
          committed_any=true
        done <<< "$all_changed"

        # 处理剩余变更（根目录文件等）
        git add -A
        if ! git diff --cached --quiet; then
          git commit -m "📦 sync: refresh packages"
          committed_any=true
        fi

        if [ "$committed_any" = "true" ]; then
          git push --quiet "https://${{ secrets.ACCESS_TOKEN }}@github.com/kenzok8/litte.git" HEAD:master
          echo "✅ Push 完成"
        else
          echo "ℹ️ nothing to commit"
        fi

    - name: Delete workflow runs
      uses: Mattraks/delete-workflow-runs@main
      with:
        retain_days: 1
        keep_minimum_runs: 1
